#!/bin/bash

# Waybar indicator for notification silencing status. Returns JSON for waybar custom module.

if makoctl mode | grep -q 'do-not-disturb'; then
  echo '{"text": "󰂛", "tooltip": "Notifications silenced", "class": "active"}'
else
  echo '{"text": ""}'
fi

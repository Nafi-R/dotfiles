#!/bin/bash

# Display a system power menu using walker dmenu.

menu() {
  local prompt="$1"
  local options="$2"

  echo -e "$options" | nafi-launch-walker --dmenu --width 295 --minheight 1 --maxheight 630 -p "$prompt" 2>/dev/null
}

options="  Lock"
options="$options\n󰒲  Suspend"
options="$options\n󰍃  Logout"
options="$options\n󰜉  Restart"
options="$options\n󰐥  Shutdown"

case $(menu "System" "$options") in
*Lock*) nafi-lock-screen ;;
*Suspend*) systemctl suspend ;;
*Logout*) loginctl terminate-user "$USER" ;;
*Restart*) systemctl reboot ;;
*Shutdown*) systemctl poweroff ;;
esac
